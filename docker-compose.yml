version: "3.9"
services:
  shellhacks:
    build:
      context: ./shellhacks
    ports:
      - ${FRONTEND_PORT}:${FRONTEND_PORT}
    env_file:
      - ./.env
  backend:
    build:
      context: ./backend
    ports:
      - ${PORT}:${PORT}
      - 5555:5555
    env_file:
      - ./.env
