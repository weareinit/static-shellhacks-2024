version: "3.9"
services:
  shellhacks:
    build:
      context: ./shellhacks
    ports:
      - ${FRONTEND_PORT}:${FRONTEND_PORT}
    env_file:
      - ./.env
    volumes:
      - ./shellhacks/src:/shellhacks/src
    command:
      - npm
      - run
      - dev
