version: "3.9"
services:
  shellhacks:
    build:
      context: ./shellhacks
    ports:
      - ${FRONTEND_PORT}:${FRONTEND_PORT}
    env_file:
      - ./.env
  backend:
    build:
      context: ./backend
    ports:
      - ${PORT}:${PORT}
    env_file:
      - ./.env
