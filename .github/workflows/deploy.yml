# Sample workflow for building and deploying a Next.js site to GitHub Pages
#
# To get started with Next.js see: https://nextjs.org/docs/getting-started
#
name: Deploy Next.js site to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["gh-pages"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: 'npm'
      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          # Automatically inject basePath in your Next.js configuration file and disable
          # server side image optimization (https://nextjs.org/docs/api-reference/next/image#unoptimized).
          static_site_generator: next
      - name: Install dependencies
        run: npm ci
      - name: Rename assets
        run: npm run rename-assets
      - name: Build with Next.js (Static Export)
        env:
          SKIP_ENV_VALIDATION: 1
          NODE_ENV: production
          NEXT_PUBLIC_BASE_PATH: /shellhacks-2024
        run: npm run build:static
      - name: Prepare deployment directory
        run: |
          # Create deployment directory
          mkdir -p deploy/shellhacks-2024
          
          # Move the build output to the correct location
          mv out/* deploy/shellhacks-2024/
          
          # Create necessary directories
          mkdir -p deploy/shellhacks-2024/_next
          mkdir -p deploy/shellhacks-2024/static/media
          
          # Copy static assets to the correct locations
          if [ -d "deploy/shellhacks-2024/static" ]; then
            cp -r deploy/shellhacks-2024/static/* deploy/shellhacks-2024/_next/static/ || true
          fi
          
          # Copy public assets
          cp -r public/* deploy/shellhacks-2024/static/media/ || true
          
          # List the final structure
          echo "Final deployment structure:"
          find deploy -type f
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./deploy

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
